#ifndef UTILS_H
#define UTILS_H

void reverse(int* nums, unsigned int size);
/*
a function that reverses an array of integers using a stack
input: the nums array and its size
output: none
*/
int* reverse10();
/*
a function that gets 10 numbers from the user and reverses them
input: none
output: a pointer to the reversed array
*/

#endif // UTILS_H